module.exports = {
    experimental: {
        turbo: false,
    },
};
/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        turboMode: false,  // turboMode を false にする
    },
};

module.exports = nextConfig;
